# Git
.git
.gitignore

# Python
__pycache__/
*.py[oc]
.venv/
*.egg-info/

# Data files (mounted as volumes at runtime — not baked into the image)
data/
data_cleaned/
chroma_db/
openclaw/charts/
openclaw/exports/
openclaw/media/
openclaw/logs/

# OpenClaw project-local state (created at runtime / mounted as a volume)
.openclaw/
openclaw/.openclaw/
openclaw/openclaw.json.bak*
openclaw/openclaw.json.last-good

# Secrets
.env

# Node
node_modules/
skillhub/frontend/node_modules/

# Frontend build output (built inside Docker, not from host)
skillhub/frontend/dist/

# Power BI / misc
*.pbix
*.pbit
*.pdf
